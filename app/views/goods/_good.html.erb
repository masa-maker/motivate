
  <% if post.liked_by?(current_user) %>
    <%= link_to "いいね外す", post_add_path(post), method: :delete, remote: true, id: :"like-button-#{post.id}", class:"btn-sm btn-outline-danger"%>
  <% else %>
    <%= link_to "いいねする", post_add_path(post), method: :post, remote: true, id: :"like-button-#{post.id}", class:"btn-sm btn-danger"%>
  <% end %>
